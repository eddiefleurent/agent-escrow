{
  "skip_comment": "This finding is already fixed on the current branch. `AcceptBid` now only accepts the finalized sealed-bid winner, and the regression test `TestAcceptBid_RejectsLosingEligibleBidOnInitialSealedFinalization` covers the losing-but-eligible case."
}
