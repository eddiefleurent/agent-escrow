{
  "skip_comment": "This review item is already fixed in the current branch. `updateRFQSealedBiddingStateOn` in `go-server/internal/storage/queries.go` already checks `RowsAffected()` and returns `sql.ErrNoRows` when no RFQ row is updated."
}
