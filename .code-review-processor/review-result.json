{
  "skip_comment": "Already fixed in the current tree: `TestFinalizeSealedBidding_ImmediateTxBlocksLateReveal` in `go-server/internal/bidding/bidding_test.go` already exercises `svc.FinalizeSealedBidding(...)` and no longer reproduces the internal `BeginImmediateTx` / `ExpireCommittedBidCommits` / `UpdateRFQSealedBiddingState` sequence directly. Recent history (`098a811`, `fix: cover sealed bid finalizer locking`) appears to have addressed this finding."
}
