{
  "skip_comment": "Accepted the underlying issue and removed the in-memory single-connection clamp in storage.Open. I did not implement the broader tx-only API refactor because the current evidence pointed to the pool restriction itself as the concrete problem, while sealing the entire storage surface behind a transaction-only interface would add broad churn without a demonstrated current need."
}
