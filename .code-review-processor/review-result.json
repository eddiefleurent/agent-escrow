{
  "skip_comment": "Already fixed in `ba12e1b` (`✅ test: cover sealed bid commit-time tie break`). The current `TestFinalizeSealedBidding_SelectsDeterministicBestBid` already adds a third revealed bid, controls commit ordering, asserts `summary.BestBidID`, and checks `len(summary.EligibleBidIDs) == 3`."
}
